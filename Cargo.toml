[workspace]
resolver = "2"

members = [
    "coeus-core",
    "coeus-tensor",
    "coeus-ops",
    "coeus-autograd",
    "coeus-nn",
    "coeus-optim",
    "coeus-python",
    "coeus-sparse",
    "coeus-wgpu",
    "coeus-cuda",
    "coeus-dist",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["Ryan Clanton <ryanclanton@outlook.com>"]
license = "MIT OR Apache-2.0"

[workspace.dependencies]
# ── Remote System Dependencies ──
# Parallelization/async SSOT. default-features=false so moirai does NOT impose its
# own mnemosyne #[global_allocator] on coeus (coeus allocates explicitly via
# mnemosyne in coeus-core::storage); we consume only the data-parallel surface.
moirai     = { git = "https://github.com/ryancinsight/Moirai.git", default-features = false, features = ["parallel"] }
mnemosyne  = { git = "https://github.com/ryancinsight/Mnemosyne.git" }
apollo-fft = { git = "https://github.com/ryancinsight/apollo.git", package = "apollo-fft" }
# SIMD-effect SSOT. Tracks hermes `main`; advance with `cargo update -p hermes-simd`.
hermes-simd = { git = "https://github.com/ryancinsight/hermes.git", package = "hermes-simd" }

# ── Numerical ──
num-traits = "0.2"
bytemuck   = { version = "1.13", features = ["derive", "extern_crate_std"] }
half       = { version = "2.7", features = ["num-traits", "bytemuck"] }
# No "rayon" feature: coeus uses moirai as its data-parallel runtime and calls no
# ndarray parallel iterators. (rayon may still enter the build tree transitively
# via apollo-fft, which uses ndarray's parallel `Zip`; eliminating that requires
# migrating apollo's ndarray-par sites to moirai — tracked, out of scope here.)
ndarray    = { version = "0.16" }
smallvec   = "1.13"
wgpu       = "23.0"
pollster   = "0.3"

# ── Error ──
thiserror = "2"

# ── Python ──
pyo3 = { version = "0.23", features = ["extension-module"] }

# ── Local ──
coeus-core     = { path = "coeus-core" }
coeus-tensor   = { path = "coeus-tensor" }
coeus-ops      = { path = "coeus-ops" }
coeus-autograd = { path = "coeus-autograd" }
coeus-nn       = { path = "coeus-nn" }
coeus-optim    = { path = "coeus-optim" }
coeus-sparse   = { path = "coeus-sparse" }
coeus-dist     = { path = "coeus-dist" }
