[package]
name = "jit"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
description = "JIT compilation and graph optimization for Coeus deep learning framework"
readme = "README.md"
keywords = ["deep-learning", "jit", "compiler", "optimization"]
categories = ["science", "algorithms", "compilers"]

[dependencies]
# Workspace dependencies
coeus-tensor = { path = "../tensor" }
coeus-autograd = { path = "../autograd" }
coeus-backend = { path = "../backend" }
coeus-storage = { path = "../storage" }
coeus-nn = { path = "../nn" }
coeus-dtype = { path = "../dtype" }

# Error handling
thiserror = { workspace = true }

# Collections and utilities
indexmap = "2.0"

# JIT compilation with Cranelift
cranelift = "0.102"
cranelift-module = "0.102"
cranelift-object = "0.102"
cranelift-jit = "0.102"
cranelift-codegen = "0.102"
cranelift-frontend = "0.102"

# Logging
tracing = { workspace = true }

# Target architecture detection
target-lexicon = "0.12"

# CPU detection
num_cpus = "1.16"
raw-cpuid = "11.0"

[dev-dependencies]
# Temporary files for testing
tempfile = "3.0"
