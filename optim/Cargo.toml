[package]
name = "coeus-optim"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
description = "Optimization algorithms for Coeus neural networks"
readme = "README.md"
keywords = ["machine-learning", "optimization", "adam", "sgd", "rmsprop", "rust"]
categories = ["science", "algorithms"]

[dependencies]
coeus-autograd = { path = "../autograd" }
coeus-dtype = { path = "../dtype" }
coeus-tensor = { path = "../tensor" }
coeus-backend = { path = "../backend" }
coeus-storage = { path = "../storage" }
num-traits = { workspace = true }
thiserror = { workspace = true }
serde = { version = "1.0", features = ["derive"] }
wgpu = "0.19"
bytemuck = { version = "1.13", features = ["derive"] }
futures = "0.3"

[dev-dependencies]
approx = { workspace = true }
